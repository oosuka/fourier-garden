# Fourier Garden 開発ガイド

このファイルは、リポジトリ全体に適用される開発指示である。
実装、レビュー、テスト、ドキュメント作成、コミットを行うエージェントは、
本書を最初に読み、ここに定める数学的正確性、音響品質、描画品質を守ること。

## 1. 指示の優先順位

判断が衝突した場合は、次の順序を優先する。

1. 実行環境から与えられるsystemおよびdeveloper指示
2. ユーザーが現在の依頼で明示した指示
3. この `AGENTS.md`
4. [`docs/mathematical-model.md`](docs/mathematical-model.md) の数理定義
5. [`README.md`](README.md) のプロダクト仕様
6. 既存コードとテストが示す慣例

数学的な定義については `docs/mathematical-model.md` を正本とする。
コードと文書に不一致がある場合、都合よくコードへ合わせず、数理的に正しい定義を
確認してからコード、テスト、UI、文書を一緒に修正すること。

## 2. プロダクトの定義

Fourier Gardenは、次の要素を中核とするデスクトップ向け
オーディオビジュアルWeb作品である。

- 有限フーリエ級数の合成
- 複素指数関数とフェーザによるフーリエ級数の幾何学的可視化
- 同じ数学的構造から導く音楽的ソニフィケーション
- 数学層と同期する没入型の詩的ビジュアル

本作品は、FFT（高速フーリエ変換）アルゴリズムの計算過程を可視化する作品ではない。
初期章 `Residue Bloom / 剰余の花` では係数が解析的に与えられており、
標本列からDFTまたはFFTで係数を推定していない。

UI、README、説明文、コメントでは、この区別を曖昧にしないこと。
実際にDFTやFFTを導入していない機能を「高速フーリエ解析」「FFT解析」
「FFT可視化」と呼んではならない。

## 3. 作品の優先順位

設計判断では次を重視する。

1. 数学的正確性と知的価値
2. 鮮やかで繊細な高解像度グラフィック
3. 長時間聴いても不快になりにくい音響
4. 映像、数学、音声の同期
5. デスクトップChromeでの安定性と性能
6. 将来の章を追加できる拡張性

見栄えのために数学的意味を偽ってはならない。一方で、数学的に厳密な表示だけに
作品全体を制限する必要はない。装飾表現は、その役割を正確に区別して導入すること。

### 3.1 動勢と演出の品質不変条件

数学的正確性は常に演出より優先する。ただし、厳密数学層を維持できる範囲では、
単調さの排除、躍動感、長時間の鑑賞価値を最優先する。数学的に正しいことだけを
理由に、同じ発音、同じ輝度変化、同じ運動を長時間反復する章を完成扱いしない。

公開する各章は次を満たす。

- 30秒を超える体験は、3つ以上の認識可能な時間状態または幕を持つ
- 密度、強弱、音域、音色、空間、運動、色彩のうち3軸以上に明確な変化を持つ
- 音響、映像、空間運動のすべてが実質的に無変化となる構成を禁止する
- 数学要素と局所的な発音・造形の因果を説明可能にする
- 数学層を変形せず、ソニフィケーション層と詩的造形層で動勢を作る
- 同じプロファイルの発音や全体一斉点滅へ依存せず、局所応答と時間的対比を使う
- 新章の知覚音量を公開済み章の代表区間および全周期RMSと比較し、段階1仕様で
  許容差を数値化する。比較テストなしに「十分聞こえる」と判断しない
- 個々の発音は有限包絡で減衰させながら、異なる声部と余韻を重ねて句全体の
  心地よいテンポと集合的連続性を作る。長い完全無音と唐突な断絶を演出意図なしに
  残さない
- 連続な数学量を音響・造形へ写す章では、その量を発音開始時だけ標本化して固定せず、
  絶対transport時刻で連続評価する局所制御を少なくとも1軸持たせる
- 高品質描画では主役表面だけでなく周囲空間にも十分な粒子密度と奥行きを持たせる。
  固定黒一色の背景、微小な全体運動、低opacityの粒子だけで完成扱いしない
- 背景、粒子、カメラを含む3軸以上に、常時観察できる局所的かつ非同期な変化を持たせる
- 48 kHz、128標本のAudioWorklet期限は約2.67 msである。新章は決定的な同時発音
  複雑度上限を定義し、代表block計測で期限の50%以下を目標にする
- AudioWorkletの標本ループ内でイベント全表走査、モード探索、並べ替え、一時配列・
  一時オブジェクト生成を行わず、設定時に周波数、定位、部分音重みを事前計算する
- ウルトラワイドでも左右端を固定黒の空白帯にせず、数学層と別の背景・粒子層へ
  局所的な奥行きと運動を維持する
- `PatternDramaturgy`を定義し、`validatePatternDefinition()`の幕数、連続範囲、
  表現軸、コントラスト、局所数学写像、体験品質契約の検証を通す
- 自動テストだけで作品品質を完成扱いせず、実機試聴と連続観察を公開条件にする

数学的対象そのものが遅い、疎、静的である場合も、その性質を偽ってはならない。
その場合は観察視点、比較、局所強調、時間構成、音楽的変換など、数学層の外側で
鑑賞上の動勢を設計する。題材の性質を単調な演出の免責理由にしない。

## 4. 表現レイヤー

すべての表示と音声を、次の3層のいずれかとして扱う。

### 4.1 厳密な数学層

- エピサイクル
- フェーザと接続ベクトル
- フェーザ終点
- 主履歴波形
- 解析的係数スペクトル
- 係数、位相、調波番号の表
- Dirichlet境界、固有モード波動面、節線
- 固有値軸、符号付き係数、相対エネルギー指標
- 数式と数学的解説

この層は、定義された有限フーリエ級数と数値的に一致しなければならない。
見栄えのためのノイズ、テーパー、恣意的な位相ずらし、補間誤差を混入させないこと。

### 4.2 ソニフィケーション層

音声は数学的構造を聴覚へ写す音楽的表現であり、表示級数の無加工再生ではない。
移調、知覚重み、帯域制限、エンベロープ、定位、EQ、コンプレッション、
リミッター、残響を使用してよい。

ただし、どの数学的量を維持し、どの音響処理で変形したかを説明可能にすること。
UIと文書では「解析結果そのものの音」と誤認させず、ソニフィケーションと呼ぶ。

### 4.3 詩的な造形層

- GPU粒子
- 有機膜
- 流線
- 星雲
- ブルーム
- 残光
- 二次トレイル
- 光柱、アーチ、局所ハロー

これらは数学層とtransport、焦点、色彩、呼吸周期を共有してよいが、
フーリエ級数、係数、DFT、FFTの値そのものとして説明してはならない。

## 5. Chapter 1の数学的不変条件

`Residue Bloom` の有限フーリエ級数は次式である。

```math
f(x)=5\sum_{k=0}^{12}\frac{1}{k+1}\sin((4k+1)x)
```

各項を次のように保持する。

```math
n_k=4k+1,\qquad A_k=\frac{5}{k+1},\qquad \phi_k^{(\sin)}=0
```

したがって、不変条件は次のとおりである。

- 項数は13
- 調波番号は `1, 5, 9, ..., 49`
- すべての調波番号は `n_k ≡ 1 (mod 4)` を満たす
- 正弦形式の振幅は `A_k = 5 / (k + 1)`
- 正弦形式の位相は0

一方向回転の複素フェーザを次で定義する。

```math
z(x)=\sum_{k=0}^{12}A_ke^{in_kx}
```

主波形との関係は次である。

```math
f(x)=\operatorname{Im}z(x)
```

エピサイクルの終点は `z(x)` と一致し、主履歴波形はその虚部を同一倍率で
投影したものとする。

標準的な二側複素フーリエ係数は次である。

```math
c_{n_k}=-\frac{iA_k}{2},
\qquad
c_{-n_k}=\frac{iA_k}{2}
```

UIの `A_k` は正弦形式の振幅であり、
複素係数の絶対値 `|c_{±n_k}| = A_k / 2` と混同しないこと。

画面上の観察時間は次である。

```math
x(t)=0.31t
```

これは構造を観察するための時間伸長である。画面の `55 Hz` 表記は
解析的スペクトルを音響周波数へ対応させる基準周波数であり、
画面上の基本フェーザが毎秒55回転することを意味しない。

スペクトルはFFT推定値ではなく、既知の係数を次の周波数へ配置する
解析的係数スペクトルである。

```math
f_k=n_kf_0,\qquad f_0=55\ \mathrm{Hz}
```

### 5.1 時間と反復スコアの分離

- 数学表示の時刻は絶対transport時刻に対する `x(t)=0.31t` であり、
  音楽スコアの周期でリセットしない
- 反復イベント表は区間、発音、キャリア、基礎プロファイルだけを保持する
- フェーザ座標、半径、フェーザ由来brightness・accentを反復イベントへ保存しない
- フェーザ由来制御はイベントが実際に発生する絶対イベント時刻から評価する
- ループ直後の履歴イベントは前周回の絶対イベント時刻を使う

### 5.2 スペクトルと数値描画

- 解析的スペクトルの横軸と目盛は同一の座標関数を使う
- `Residue Bloom` の片側表示振幅は正弦形式 `A_k` であり、
  `|c_{±n_k}|=A_k/2` と混同しない
- 55 Hzは解析的スペクトルの周波数対応基準であり、表示フェーザの実時間回転数や
  音声イベントのcarrier周波数と呼ばない
- 折れ線描画は解析式から得た厳密な標本点を結ぶ数値描画と説明し、
  有限線分が連続曲線そのものだと主張しない

数式、位相規約、投影軸、係数形式の変更は、局所的な描画修正として扱わないこと。
`src/math`、シーン、詳細パネル、テスト、README、数理モデル文書を同時に確認する。

## 6. 音響の不変条件

音声は低い持続音を鳴らし続ける実装にしない。一定のリズムで短く発音し、
発音間で十分に減衰する、穏やかで反復疲労の少ない音を基準とする。
ただし「減衰」を長い無音の正当化に使わない。複数声部の重なり、応答、残響尾で
句全体の連続性を保ち、通常音量で公開済み章と比較可能な明瞭さを確保する。

### 6.1 Residue Bloom

Chapter 1の音響モデルは次のとおりである。

- 基準周波数: `f₀ = 55 Hz`
- テンポ: 80 BPM
- 刻み: 16分音符
- キャリア列: `9f₀, 8f₀, 8f₀, 9f₀`、すなわち `495, 440, 440, 495 Hz`
- 調波指数: 数学層と同じ `n_k = 4k + 1`
- 知覚重み: `A_k / (k + 1)^1.85`
- ステレオデチューン比: `d = 0.00125`
- 帯域条件: `max(n_k ν_j(1-d), n_k ν_j(1+d)) < 0.45 F_s`
- 共通音質: dry high-shelf `1,250 Hz / -16 dB`、dry low-pass `2,100 Hz`、
  wet low-pass `1,450 Hz`、`-1 dBFS` limiterを使い、Chapter 2/3と同じ
  丸い中域ピコ系へ寄せる
- 4音アクセントはsectionごとに変える。`intro`は`[1.00, 0.64, 0.90, 0.72]`、
  `growth`は`[0.90, 0.74, 1.08, 0.66]`、`bloom`は`[1.10, 0.70, 0.94, 0.82]`、
  `hush`は`[0.72, 0.58, 0.84, 0.62]`、`return`は`[0.92, 0.66, 1.00, 0.78]`を
  基準にする。section内の小節番号で4音アクセントを循環ローテーションし、
  各小節のdownbeatだけ1.08倍する。さらに16ステップのゴースト輪郭
  `[1.14, 0.48, 0.82, 0.52, 1.00, 0.46, 0.78, 0.50, 1.06, 0.48, 0.80, 0.54, 0.96, 0.46, 0.74, 0.50]`を
  小節内で3ステップずつ循環させ、評価済みアクセントを`0.30`から`1.36`へ制限する。
  全16分を鳴らしつつ、Chapter 1は強拍アンカーとゴースト粒による聴感上のリズム変化を持たせる。
  さらに評価済みアクセントを基礎brightnessと減衰倍率へ写し、強拍は明るく長く、
  弱拍は暗く短く聴こえるようにする

帯域条件はナイキスト周波数に対して余裕を持たせるために必須である。
サンプルレートに依存せず、ナイキスト周波数を超える成分やその近傍の危険な成分を
生成しないこと。

音響実装では次を守る。

- `AudioWorklet` 内でサンプル精度の時間を用いる
- 映像と音声は単一transportを共有する
- 音声開始後は `AudioContext.currentTime` を同期基準にする
- 開始、停止、再開、タブ非表示からの復帰でクリックノイズを出さない
- 停止時とタブ非表示時は短いフェードを使う
- DC成分、過大な低域、過大な高域、エイリアシングを避ける
- リミッター出力は原則として `-1 dBFS` 以下に収める
- 初期音量は35%とし、ユーザー変更値をローカル保存する
- ヘッドホンを主基準とし、Mac内蔵スピーカーでも破綻しないことを確認する

### 6.2 Spectral Cathedral

Chapter 2は72 BPM、5/4拍子、18小節、75秒、360イベントの5幕スコアを使う。
全小節の全16分slotを発音し、密度変化でなく一定リズムを不変条件にする。
固有値平方根の順序、係数絶対値比、係数符号の開始位相を保持し、420-980 Hzの
安全な中域へ正規化した単一部分音の短いピコ粒へ写す。全イベントの
register倍率は1、最大部分音数は1、左右デチューン後の各生成周波数は
`0.45 F_s`未満に制限する。Chapter 2は
dry high-shelf 1,200 Hz / -18 dB、dry low-pass 1,300 Hz、wet low-pass
1,050 Hz、短い包絡、低いwet gainを基準にし、乾いた幾何学的なピコ粒として
Chapter 3より短く中央寄りに保つ。スコア上のwet sendは最大0.055、stereo spreadは
最大0.38、4 slotアクセントは`[1.00, 0.70, 0.96, 0.66]`を基準にし、
Chapter 3の流動的な定位と混同しない乾いた格子感を保つ。高域抑制を弱める場合は参照動画方向の
セルフチェック数値と実機試聴で証明する。
数学時刻は75秒周期でリセットせず、前周期の余韻は絶対イベント時刻から評価する。

音響式またはDSPを変える場合は、
`src/patterns/spectral-cathedral/audio/score.ts`、
`src/patterns/spectral-cathedral/audio/synthesis.ts`、
`public/audio/chapters/spectral-cathedral.js`、`public/audio/fourier-worklet.js`、
`src/audio/AudioEngine.ts`の責務を確認し、TypeScript参照実装とAudioWorkletの同じ定義が
食い違わないようにすること。

### 6.3 Möbius Choir

Chapter 3は68 BPM、16小節、56.470588秒、256イベントの5幕スコアを使う。
全小節の全16分slotを発音し、密度変化でなく一定リズムを不変条件にする。
固有値平方根の順序、基礎振幅比、許容条件、`n>0`の正弦・余弦対の位相関係を
保持し、420-920 Hzの安全な中域へ正規化した単一部分音の短いピコ粒へ写す。
全イベントのregister倍率は1、最大部分音数は1、追加発音オフセットは0のみ、
ノイズ状付加音源とフォルマント系付加音のゲインは0を基準にする。
dry high-shelf 1,000 Hz / -24 dB、dry low-pass 1,080 Hz、wet low-pass 860 Hzを
基準とし、Chapter 2より長い包絡、高いwet gain、広い定位で柔らかく流れる
ピコ粒として保つ。スコア上のpan motionは最小0.32、最大0.72以上の山を持ち、
4 slotアクセントは`[0.78, 1.00, 0.68, 0.92]`を基準にして、Chapter 2より
左右へ流れる帯状のキャラクターにする。音響正規化後の出力ゲインは0.36を基準にし、
広い定位で薄くなりすぎないようにする。高域抑制を弱める場合は参照動画方向の
セルフチェック数値と実機試聴で証明する。carrierはevent ageで再始動せず絶対transport時刻で評価し、
flat quotientのモード変位と速度を振幅、定位、詩的造形の局所応答へ連続写像する。
個々の発音は有限包絡で閉じ、前周期の余韻は絶対イベント時刻から評価する。
TypeScript参照DSPとAudioWorkletを標本単位で一致させ、左右デチューン後の
全生成周波数を`0.45 F_s`未満に制限する。

音響式またはDSPを変える場合は、`src/patterns/mobius-choir/audio/score.ts`、
`src/patterns/mobius-choir/audio/runtime.ts`、
`src/patterns/mobius-choir/audio/synthesis.ts`、
`public/audio/chapters/mobius-choir.js`、`public/audio/fourier-worklet.js`、
`src/audio/AudioEngine.ts`を同時に確認する。

## 7. 対象環境と技術スタック

対象はデスクトップのみとする。スマートフォン対応は初期版の範囲外である。

- OS: 最新版macOS
- ブラウザ: 最新版Google Chrome
- 基準機: MacBook Air M2、10-core GPU、16GB RAM
- ランタイム管理: Volta
- Node.js: `24.16.0`
- パッケージ管理: npm `11.17.0`
- ビルド: Vite 8
- UI: React 19、TypeScript 6
- 描画: Three.js r184、WebGPURenderer、TSL
- フォールバック: WebGL2
- 数式表示: KaTeX
- 音声: Web Audio API、AudioWorklet
- テスト: Vitest
- lint: Oxlint
- format: Biome

依存関係のバージョンは `package.json` と `package-lock.json` を正本とする。
Node.jsとnpmのバージョンは `package.json` の `volta` フィールドを正本とする。
このリポジトリではVoltaを使い、nvm、asdf、mise、Corepackなど別の管理設定を
追加しない。Node.js 24系またはnpm 11系を更新するときは、各系列の最新安定版を
確認し、`volta pin node@<version> npm@<version>` で厳密なバージョンを固定する。
`engines`、`packageManager`、README、AGENTS.mdも同時に更新する。

依存パッケージのinstall scriptは `.npmrc` の `strict-allow-scripts=true` で
未審査の実行を拒否する。必要なinstall scriptは、内容と導入経路を確認してから
`npm approve-scripts <package>` で承認し、`package.json` の `allowScripts` に
厳密なバージョンを記録する。依存更新でバージョンが変わった場合は自動承認せず、
再審査すること。`--no-allow-scripts-pin`、`npm approve-scripts --all`、
`dangerously-allow-all-scripts=true` を通常運用で使用しない。

新規依存を追加する前に、標準APIまたは既存依存で解決できないか確認する。
描画やDSPの中核依存を変更する場合は、品質、バンドルサイズ、Chrome対応、
メンテナンス状況を説明できること。

フォントはオープンライセンスの書体をセルフホストし、実行時に外部CDNへ依存しない。

## 8. リポジトリ構成

主な責務は次のとおりである。

- `src/math/`: 複数章で意味が同じ級数、係数、フェーザなどの純粋数学演算
- `src/audio/`: `AudioEngine`、章非依存のAudioGraphとWorklet program契約
- `public/audio/fourier-worklet.js`: AudioWorkletの共通lifecycleと章processor dispatcher
- `public/audio/chapters/`: AudioWorkletの章別検証、状態、標本processor
- `src/core/`: トランスポート、品質制御、シード、レンダラーバックエンド
- `src/patterns/contracts.ts`: 全章共通のdefinition、scene、dramaturgy、表示契約
- `src/patterns/registry.ts`: published/preview章の登録点
- `src/patterns/<chapter-id>/`: 章固有のdefinition、数学、音響、scene、詳細UI、QA、テスト
- `src/components/`: 章非依存のReact UIと表示用キャンバス
- `src/styles.css`: 全体レイアウト、タイポグラフィ、共通UI表現
- `.oxlintrc.json`: Oxlintの規則、環境、ファイル単位の例外
- `biome.json`: Biomeのフォーマッター設定
- `docs/mathematical-model.md`: 数学とソニフィケーションの正本
- `design-qa.md`: 視覚QAの条件、証拠、残課題
- `README.md`: 利用者向けの概要、操作、開発方法

数学処理は可能な限りDOM、Three.js、Web Audio APIから独立した純粋関数にし、
単体テスト可能にする。Reactコンポーネントへ数式やDSP定義を直接埋め込まない。

## 9. パターンアーキテクチャ

章は `src/patterns/registry.ts` のレジストリから生成する。
章番号、タイトル、数式、音響プリセット、教育コンテンツ、シーンの遅延ロードを
個別のハードコードで増やさないこと。

各パターンは、概念的に次の契約を守る。

```ts
interface PatternDefinition {
  id: string;
  kind: string;
  order: number;
  publication: "published" | "preview";
  title: LocalizedText;
  dramaturgy: PatternDramaturgy;
  audio: PatternAudioPreset;
  education: EducationContent;
  MathematicalDetails: ComponentType;
  validate(): void;
  loadScene(): Promise<PatternSceneFactory>;
}

interface PatternScene {
  update(frame: FrameContext): void;
  resize(viewport: Viewport): void;
  setQuality(level: QualityLevel): void;
  dispose(): void;
}
```

新しい章を追加するときは次を満たす。

- `src/patterns/<chapter-id>/`へ`definition.tsx`、`types.ts`、`validate.ts`、`math/`、
  `audio/`、`scene/`、`details/`、必要な`qa/`とテストを集約する
- 共有実装から章実装をimportせず、ある章から別の章をimportしない
- 数学的対象と操作を明記する
- 係数が解析的か、DFT/FFT推定かを明記する
- 投影軸と位相規約を明記する
- 音声が原信号、変換結果、ソニフィケーションのどれかを明記する
- 数学時刻が絶対時刻か周期時刻かを明記する
- 音楽スコアの周期が数学時刻と独立に反復するかを明記する
- スペクトルの振幅規約と横軸スケールを明記する
- 厳密な数学層と詩的造形層を区別する
- シーンを遅延ロードする
- `dispose()` でGPUリソースとイベント購読を解放する
- レジストリと数学のテストを追加する
- デチューン後の実生成周波数に対する帯域制限テストを追加する
- `public/audio/chapters/<chapter-id>.js`へWorklet processorを実装し、共通dispatcherへ登録する
- `validatePatternDefinition()` の登録時検証を通す
- やさしい説明と数学の詳細を両方用意する

将来章の候補はREADMEに記載されているが、名称だけを根拠に数学的内容を実装しない。
各章について数理定義を先に確定してから制作する。

## 10. 描画品質

グラフィックは作品の中心であり、次を必須品質とする。

- 深い黒を確保し、全画面を均等に発光させない
- 細線は高DPIで明瞭に表示する
- シアン、バイオレット、暖色の金を階調豊かに使う
- ブルームで白飛びさせず、発光中心と周辺光を分離する
- 粒子密度、線幅、残光、膜の透明度に奥行きを持たせる
- 数学線、文字、UIは常にネイティブ解像度を維持する
- フレーム間で急な位置、色、輝度の跳躍を発生させない
- 主構図を維持し、装飾が数式や主波形の可読性を奪わない

主構図は固定し、粒子配置、色相、呼吸周期のみをローカル日付由来のシードで
緩やかに変奏する。テストと視覚QAでは固定シードを使用し、同一フレームを
再現可能にする。

対応アスペクト比は16:10、16:9、ウルトラワイドとする。
単純な中央クロップではなく、カメラ、焦点、波形長、詳細パネル、操作UIを
連続的に再配置する。

## 11. rendererとリソース管理

- 通常は `WebGPURenderer` とTSLを使用する
- WebGPU非対応時は専用の `WebGLRenderer` へフォールバックする
- `?renderer=webgl` でWebGL2経路を強制確認できる状態を維持する
- `?seed=qa` で固定シードを使用できる状態を維持する
- `?quality=high` で高品質を固定できる状態を維持する
- タブ非表示時は描画を停止する
- WebGPU device loss時は描画基盤を再初期化する
- WebGL context loss/restorationを安全に処理する
- scene切り替え、再初期化、unmount時にGPUリソースを破棄する
- animation frame、DOMイベント、AudioNode、タイマーを残留させない

WebGPUとWebGL2は別実装上の制約を持つ。片方で動くThree.jsオブジェクトや
マテリアルが他方でも動くと仮定せず、両経路を実際に確認すること。

## 12. 性能と適応品質

基準ディスプレイ `2880 x 1864` で60fps維持を必須目標とする。
さらに `3840 x 2160` の固定ビューポートで60秒間計測し、平均60fpsを目標とする。
120fpsは対応ディスプレイでの適応目標であり、初期版の必須条件ではない。

負荷時は次の順序を基本に段階的に品質を下げる。

1. 粒子数
2. 有機膜の内部解像度または複雑度
3. ブルームの内部解像度とサンプル量
4. 残光レイヤーの内部解像度

数学線、文字、UIの解像度を最初に下げてはならない。
毎フレームのReact state更新、不要な配列確保、GPUバッファ再生成、
シェーダー再コンパイルを避ける。

性能修正では見た目だけで判断せず、フレーム時間、GPU/CPU負荷、
メモリ増加、長時間実行時の劣化を測定する。

## 13. UIと体験

初期画面ではユーザー操作後にのみ音声を開始する。
`ENTER FOURIER GARDEN` は作品と音声の開始だけを行い、
全画面化は専用ボタンで行う。

主要操作は次に限定する。

- 開始
- 再生 / 一時停止
- 音量
- 詳細パネル
- 全画面

キーボード操作を維持する。

- `Space`: 再生 / 一時停止
- `D`: 詳細パネル
- `F`: 全画面

無操作時は操作UIを隠して没入感を優先し、操作再開時に復帰させる。
フォーカス表示、ボタン名、ARIA属性、キーボード操作を壊さないこと。

詳細パネルには次の2種類の説明を用意する。

- 数学を知らない人向けの、比喩を用いたやさしい説明
- 定義、係数、位相、投影、スペクトルを正確に述べる数学の詳細

やさしい説明でも、FFTや周波数解析について事実と異なる簡略化をしない。

## 14. コーディング規約

- TypeScriptのstrictな型検査を維持する
- `any`、無根拠な型アサーション、エラー握りつぶしを避ける
- 数学とDSPには単位が分かる名前を使う
- 位相がradか、周波数がHzか、時刻が秒かを曖昧にしない
- マジックナンバーは、数学定義、音響プリセット、品質設定へ集約する
- 複雑な式には「何をしているか」ではなく「なぜ必要か」を短くコメントする
- Reactのレンダー内でGPUオブジェクトやAudioNodeを生成しない
- 公開インターフェースを変える場合は全呼び出し元とテストを確認する
- 既存の責務境界と命名を優先し、不要な抽象化や大規模リファクタを行わない
- ファイルは原則ASCIIを使い、日本語文書やUI文言ではUTF-8を使用する
- JS、TS、TSX、JSON、CSS、HTMLはBiomeの出力を正とする
- lint規則はOxlintへ集約し、Biomeのlinterを有効にしない
- formatとlintの責務が重複する規則を追加しない
- Oxlintの例外は誤検知する最小のファイルと規則へ限定し、理由を説明可能にする

現行のOxlint type-aware lintingはTypeScript 7以上を必要とするため、
TypeScript 6を使用している間は有効化しない。型検査は `tsc -b` で行う。
TypeScript 7へ更新するときに `oxlint-tsgolint` の安定性と互換性を再評価する。

生成物、キャッシュ、ローカル専用ファイル、巨大な録画をリポジトリへ追加しない。
秘密情報、APIキー、個人情報、ローカル絶対パスをソースへ埋め込まない。

## 15. テスト方針

変更のリスクに応じてテストを追加する。最低限、次を維持する。

### 数学

- 13項の調波番号、振幅、位相
- 直接評価した `f(x)` とフェーザ終点の虚部の一致
- 二側複素係数と共役対称性
- 主波形の投影倍率とフェーザ終点の一致
- 解析的スペクトルの周波数と振幅
- 数学時刻が音楽スコア周期でリセットされないこと
- 章定義の数学的由来とスコア写像が一致すること

### 音響

- 調波指数と加工前振幅比
- 知覚重み
- サンプルレート別かつ左右デチューン後の帯域制限
- 発音間で十分に減衰すること
- 開始、停止、再開時に不連続がないこと
- 出力ピークが規定値を超えないこと
- 不快な低い持続音へ退行しないこと

### アプリケーション

- transportの開始、停止、再開、同期
- シードの決定性
- 適応品質の遷移
- レンダラーバックエンドの選択とフォールバック
- パターンレジストリの順序とメタデータ

バグ修正では、可能な限り先に再現テストを追加し、そのテストが修正前に失敗し、
修正後に成功することを確認する。

## 16. 実行コマンド

初回セットアップ:

```bash
node --version
npm --version
npm install
```

上記はそれぞれ `v24.16.0` と `11.17.0` を返さなければならない。
異なる場合は、Voltaがインストールされ、Volta shimが `PATH` の先頭側にあるかを
確認する。

依存更新後に未審査のinstall scriptが検出された場合:

```bash
npm approve-scripts --allow-scripts-pending
npm explain <package>
npm view <package>@<version> scripts
npm approve-scripts <package>
```

不要または信頼できないスクリプトは `npm deny-scripts <package>` で拒否する。

開発:

```bash
npm run dev
```

標準検証:

```bash
npm run format:check
npm run lint
npm run typecheck
npm test
npm run build
```

一括検証:

```bash
npm run check
```

自動修正:

```bash
npm run format
npm run lint:fix
```

`lint:fix` は安全な修正だけを適用する。挙動を変える可能性がある
`--fix-suggestions` と `--fix-dangerously` は、差分を個別に検証できる場合を除き
使用しない。

変更完了前に、原則として標準検証をすべて実行する。
実行できない項目がある場合は、理由と未検証のリスクを最終報告に明記する。

## 17. ブラウザQA

描画、音響、UI、レンダラー、レイアウトを変更した場合、単体テストだけで完了としない。
最新版Chromeでローカルアプリを開き、対象操作を実行して確認する。

最低限の確認項目:

- 初回開始
- 再生、一時停止、再開
- 音量変更と再読み込み後の復元
- 詳細パネルの開閉とタブ切り替え
- 全画面への遷移と解除
- タブ非表示からの復帰
- WebGPU経路
- `?renderer=webgl` のWebGL2経路
- `?seed=qa&quality=high` の固定QA状態
- 16:10、16:9、ウルトラワイド
- コンソールエラーと未処理Promise rejectionがないこと

音響変更では実機で試聴する。自動テストの波形だけで「心地よい」と断定しない。
少なくともヘッドホンとMac内蔵スピーカーで確認し、可能なら10分以上連続再生する。

## 18. 視覚QA

添付された参照画像は、ピクセル単位の複製元ではなくアートディレクションの基準である。
固定シードと固定ビューポートでスクリーンショットを取得し、次を比較する。

- 黒の量と余白
- 主焦点と視線誘導
- 線幅と高DPIでの明瞭さ
- 粒子密度と奥行き
- 発光中心、ブルーム、白飛び
- シアン、紫、金の階調
- 数学情報と装飾の優先順位
- 詳細パネルの可読性
- 操作UIの存在感と没入感の両立

視覚QAの結果、条件、証拠、既知の残課題は `design-qa.md` に記録する。
一時ファイルのパスだけを恒久的な証拠として依存しない。

## 19. ドキュメント同期

リポジトリ内の説明文書は日本語を基本とする。コード識別子、コマンド、API名、
製品名、数式、画面に実在する英語ラベルは原表記を維持してよいが、見出しや
説明文を理由なく英語と日本語で混在させない。過去の設計書や実装計画を残す場合は、
現行仕様か履歴資料か、後続文書に置き換わった条件があるかを冒頭で明記する。

次の変更では文書更新が必要か必ず確認する。

- 数式、係数、位相、投影規約
- スペクトルの意味
- 音響モデル、テンポ、キャリア、知覚重み
- 数学層と装飾層の境界
- 操作方法
- 対象環境
- URLクエリ
- パターンレジストリ
- 性能目標またはQA条件

更新先の目安:

- 数学的定義: `docs/mathematical-model.md`
- 利用者向け概要と操作: `README.md`
- 視覚的な検証結果: `design-qa.md`
- 廃止済み設計・実装計画の履歴索引: `docs/superpowers/README.md`

文書は、数学を知らない読者への説明と、専門家が検証できる定義を混同せず併記する。
READMEは特定章の実装詳細を全体説明として扱わず、現行章一覧、全章共通契約、操作、
開発入口、正本文書への索引を中心にする。将来章の固定一覧はChapter Atlasへ集約し、
章数の増加でREADME全体を組み替えない。

## 20. 開発ワークフロー

1. 依頼と関連文書を読む
2. `git status` でユーザーの未コミット変更を確認する
3. 関連コード、テスト、責務境界を調査する
4. バグでは原因を再現し、推測だけで修正しない
5. 挙動変更ではテストを先に追加または更新する
6. 変更範囲を必要最小限に保って実装する
7. `npm run format` で機械整形する
8. `npm run check` でformat、lint、テスト、型検査、buildを検証する
9. 必要に応じてChromeで実機QAを行う
10. ドキュメント同期と `git diff --check` を確認する
11. 変更内容、検証結果、残課題を簡潔に報告する

ユーザーが作成した変更を無断で戻さない。作業ツリーがdirtyな場合は、
既存変更を読み分け、自分の変更と共存させる。破壊的なGit操作を行わない。

## 21. Codexの利用方針

メイン実装は単一エージェントを基本とする。

通常はreasoning effort「中」を使用する対象:

- UI
- テスト
- 解説文
- 明確なバグ修正
- 視覚調整
- 局所的なリファクタ

reasoning effort「非常に高い」を検討する対象:

1. 描画、トランスポート、音声の境界設計
2. WebGPU、TSL基盤の初回実装または大幅変更
3. AudioWorkletと映像の同期
4. 原因不明の描画、音声、性能問題
5. 完成前の設計、数学、DSP、性能レビュー

中核実装中はサブエージェントを常用しない。独立した監査が必要な完成段階に限り、
最大3体の読み取り専用サブエージェントへ数学、DSP、GPU性能を分担できる。
サブエージェントによる編集、再帰的なエージェント生成、同じ調査の重複は禁止する。
修正判断と統合はメインエージェントが行う。

## 22. セキュリティ、プライバシー、権利

- 外部動画、音源、画像を無断転載しない
- 映像と音声は原則としてリアルタイム生成する
- 外部通信、分析トラッキング、ユーザー識別を初期版へ追加しない
- `localStorage` には音量など非機密の設定だけを保存する
- ユーザー入力を追加する場合はDOMへ未検証のHTMLを挿入しない
- 新規アセットとフォントのライセンスを確認する
- 依存パッケージの更新では破壊的変更と既知の脆弱性を確認する

## 23. 禁止事項

- 本作品をFFT計算過程の可視化と誤って説明すること
- 厳密な主波形へ装飾ノイズや恣意的変形を混ぜること
- ソニフィケーションを元級数の無加工音声と説明すること
- ナイキスト条件を無視して高調波を生成すること
- 低い持続音だけを鳴らす音響へ退行させること
- 数学線や文字を性能低下時の最初の削減対象にすること
- WebGPUだけを確認してWebGL2フォールバックを壊すこと
- 毎フレームReact stateを更新してアニメーションを駆動すること
- GPU、AudioNode、イベント、タイマーを破棄せず残すこと
- テストを削除または弱体化して失敗を隠すこと
- ユーザーの既存変更を無断で破棄すること
- 秘密情報、ローカル絶対パス、一時QAファイルをコミットすること
- 要求のないスマートフォン対応、係数エディター、アカウント、分析機能を追加すること

## 24. コミット規約

Codex Appのコミット機能、またはプロンプトでのコミット指示に応じて
コミットメッセージを作成する場合、必ず日本語の1行だけにすること。

禁止:

- 英語のコミットメッセージ
- 複数行のコミットメッセージ
- コミット本文
- 箇条書き

例:

```text
フーリエ表現の数学的整合性と音響定義を修正
```

コミット前にステージ対象を確認し、無関係な変更、生成物、秘密情報を含めない。
ユーザーが明示しない限り、勝手にコミット、プッシュ、ブランチ作成を行わない。

## 25. 完了条件

変更は、該当する次の条件を満たしたときに完了とみなす。

- 要求された挙動が実装されている
- 数学的説明と実装が一致している
- 数学層、ソニフィケーション層、詩的造形層の区別が維持されている
- 回帰テストまたは適切な新規テストがある
- Biomeのformat checkが成功する
- Oxlintが警告を含めて成功する
- typecheck、テスト、production buildが成功する
- 描画、音響、UI変更はChromeで確認されている
- WebGPUとWebGL2の該当経路が確認されている
- 長時間再生で音響とメモリが破綻しない
- 関連文書が更新されている
- `git diff --check` が成功する
- 未検証事項と残存リスクが明示されている
